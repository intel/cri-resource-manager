apiVersion: v1
kind: Pod
metadata:
  name: ${NAME}
  labels:
    app: ${NAME}
spec:
  containers:
  - image: busybox
    command:
      - sh
      - -c
      - echo ${NAME}c0 \$(sleep inf)
    imagePullPolicy: IfNotPresent
    name: busybox
  terminationGracePeriodSeconds: 1
